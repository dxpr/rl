name: Review

on: [pull_request]

env:
  TARGET_DRUPAL_CORE_VERSION: 11

jobs:
  drupal-lint:
    runs-on: ubicloud-standard-4
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v6

    - name: Lint Drupal
      run: |
        docker compose --profile lint run drupal-lint

  drupal-check:
    runs-on: ubicloud-standard-4
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v6

    - name: Check Drupal compatibility
      run: |
        docker compose --profile lint run drupal-check

  drush-e2e:
    runs-on: ubicloud-standard-4
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v6

    - name: Run Drush E2E tests
      run: |
        docker compose --profile test run --rm e2e-test

  phpunit-tests:
    runs-on: ubicloud-standard-4
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v6

    - name: Run PHPUnit unit and kernel tests
      run: |
        docker compose --profile test run --rm phpunit-tests
